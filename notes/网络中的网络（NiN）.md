# NiN块
- 由卷积层和若干个1×1的卷积层组成。
- 1×1卷积层充当了全连接层的作用，可以认为是对同一高宽上不同通道的特征做全连接。这样做的好处是显著的降低了模型的复杂度，同时不会丢失二维特征。当然，这样做在一定程度上可能会增加模型训练时长。
# NiN网络
- 使用卷积核分别为11×11，5×5，3×3的NiN块，并在每一个块之后跟一个最大池化层，窗口为3×3，步幅为2。
- 该网络用一个NiN块代替Alexnet中最后的全连接层，其输出通道数为标签类别数。然后使用全局池化层得到1×类别数×1×1的输出。