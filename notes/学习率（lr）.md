- 学习率一般和优化器一起工作。
- 学习参数时，往往需要通过梯度来更新参数使损失减小。每次更新的大小就是学习率。可以形象理解为：梯度指明方向，而学习率时迈步的大小。
- 可以把训练函数看成是一个倒立的圆锥。初始我们往往在离锥底很远的侧壁上，每一次计算所在高度（损失），都可以算出能最快到达锥底的路线（梯度）。我们要选择迈步的大小（学习率），是选择一次跨一大步还是一次挪一点点。跨一大步容易发生错过锥底的情况，而一点点挪则费时费力，因此选择一个合适的学习率对于模型而言是非常重要的。