- 作为一个训练函数，有一些最重要的也是最基础的传入参数：[[训练数据集]]，[[训练轮数（epochs）]]，[[网络（net）]]， [[损失函数（loss）]]以及[[优化器（optimizer）]]。
- 训练器的核心职责是训练网络里面的参数，使得能够较好的完成任务，如分类，回归等等。
- 核心过程：前向传播，计算损失，并反向传播计算梯度，通过优化器修改参数使得损失减小，然后重复上述过程数遍。
